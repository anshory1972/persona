# Copy this file to secrets.toml (same folder) and fill in your real key.
# secrets.toml is where Streamlit reads st.secrets from -- never commit the
# real file, only this .example template.
ANTHROPIC_API_KEY = "sk-ant-..."
